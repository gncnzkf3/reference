@import "reset.css";

.marquee_wrap {
  overflow: hidden;
}
.marquee_wrap .marquee_slide {
  display: flex;
  white-space: nowrap;
  width: 100%;
  transition-timing-function: linear;
}
.marquee_wrap .marquee_slide.mobile {
  justify-content: flex-end;
}

.marquee_wrap .marquee_slide .slide {
  height: 44px;
  padding: 13px 200px 14px 200px;
  border: 1px solid #000;
  flex-shrink: 0;
  line-height: 16px;
  box-sizing: border-box;
}
.marquee_slide .slide + .slide {
  margin-left: 10px;
}
.marquee_control {
  display: flex;
  justify-content: center;
  margin-top: 10px;
}
.marquee_control .control_btn + .control_btn {
  margin-left: 10px;
}
